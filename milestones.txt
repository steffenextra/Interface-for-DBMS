12.11.18 - 26.11.18

Wichtig: Die Funktionsweise der Methoden und die Handhabung der 
         Vektoren sollen A. in der Methode erklärt werden und B. in der Doku 
---------------------------------------------------------------------------
PROJEKTMANAGEMENT STUFF
    Gliederung der Doku
    MTA's
---------------------------------------------------------------------------
More SELECT requests --> in progress
finish Methods
{
    -SELECT requests (ASC/DESC) GROUP BY // done
    -fix the showTables method // done
    -PRIMARY KEY ->  NEW SET // done
    -SECONDARY KEY // done
    -"WHERE" Method that displayed all Columns // done
    -"WHERE" Method that displayed all Columuns with an Order (DESC,ASC) //done
    -"Your own SQL Command Method" // done 
    -method table rename 
    -method database rename //done
    -SELECT Limit          //done
}

Missing Methods
{
    ORDER BY OFFSET
    SELECT DISTINCT
    SELECT MIN and MAX
    SELECT COUNT 
    The Where with AND, OR, NOT
    WHERE BETWEEN
    WHERE in
    WHERE in
    WHERE is NULL
    GROUP BY
    HAVING
    ALIAS
    JOIN , LEFT JOIN, RIGHT JOIN, FULL JOIN, SELF JOIN
}




